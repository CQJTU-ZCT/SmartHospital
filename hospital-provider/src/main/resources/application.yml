

server:
  port: 8082



spring:
    application:
      name: UserProvider
    mvc:
      throw-exception-if-no-handler-found: true #当出现404的时候直接抛出异常
    resources:
      add-mappings: false
    profiles:
      include:
      - datasource
      - mybatis
      - eureka