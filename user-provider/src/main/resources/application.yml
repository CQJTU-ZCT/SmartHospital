

server:
  port: 8082



spring:

    application:
      name: UserProvider
    mvc:
      throw-exception-if-no-handler-found: true #当出现404的时候直接抛出异常
    resources:
      add-mappings: false
    profiles:
      include:
      - mybatis
      - eureka
      - datasource
      - common
      - sso

hospitalAdmin:
  code: 3 #标识医院管理员的用户类别编码
hospitalDoctor:
  code: 2

